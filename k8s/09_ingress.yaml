apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-app-ingress
  labels:
    name: go-app-ingress
spec:
  defaultBackend:
    service:
      name: frontend-service
      port:
        number: 8080

